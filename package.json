{
    "scripts": {
        "dev": "concurrently -n WEB,API -c blue,green \"cd frontend && bun run dev\" \"cd backend && bun run dev\"",
        "build": "cd frontend && npm run build"
    },
    "devDependencies": {
      "concurrently": "^9.0.0"
    }
  }
  